# ===== qxx deploy 连接配置 =====
# 复制本文件为 deploy/.env 并填写真实值（deploy/.env 已被 .gitignore 忽略，不会进入仓库）
# 或在执行前设置同名环境变量（优先级更高）

# VPS IP / 域名
VPS_HOST=your_vps_ip_or_host
# SSH 用户（一般 root）
VPS_USER=root
# SSH 密码
VPS_PASS=your_vps_password
# 远端部署目录
VPS_REMOTE_DIR=/root/qxx